/*
 * Landing page — intentionally blank while the designer builds the real one.
 * Just the wordmark, centered on the boba-cream ground. Nothing else.
 */
export default function Home() {
  return (
    <main className="hd hd-blank">
      <h1 className="hd-brand-word" style={{ fontSize: 40, transform: "rotate(-1.4deg)" }}>
        BOBA <span>BASH</span>
      </h1>
    </main>
  );
}
